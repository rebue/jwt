server:
  port: 0

spring:
  application:
    name: jwt-svr
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        # 默认是properties的方式
        file-extension: yaml
        # 配置分组(不设置默认为DEFAULT_GROUP)
        group: REBUE